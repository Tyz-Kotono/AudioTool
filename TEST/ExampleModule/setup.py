from setuptools import setup, find_packages

setup(
    name="QTWindowWindow",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Your Name",
    description="A template Python package for a Qt GUI project",
    python_requires=">=3.6",
)


# pip install -e .
# pip uninstall QTWindowWindow